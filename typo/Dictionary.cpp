 #include "Dictionary.h"

Dictionary::Dictionary(std::istream& stream){
    
};

Heap Dictionary::correct(const std::vector<Point>& points, size_t maxcount, float cutoff) const {
    Heap* heap = new Heap(maxcount);

    return *heap;
};